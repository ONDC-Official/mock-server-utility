default:
  callback: "on_select"
  uri:
    $ref: ""
  delay: 300
  payload:
    $ref: "./default/template/on_select.yaml"

personal-loan:
  condition:
    operation:
      type: EQUAL
      title: Check search message code
      input:
        value:
          - operation:
              type: READ
              input:
                value: "req_body.message.order.items.0.id"
          - "1234"
  callback: "on_select"
  uri:
    $ref: ""
  delay: 300
  payload:
    $ref: "./personal-loan/template/on_select.yaml"

invoice-based-loans:
  condition:
    operation:
      type: EQUAL
      title: Check search message code
      input:
        value:
          - operation:
              type: READ
              input:
                value: "req_body.message.order.items.0.id"
          - "12345"
  callback: "on_select"
  uri:
    $ref: ""
  delay: 300
  payload:
    $ref: "./invoice-based-loans/template/on_select.yaml"

