services:
  mock-server:
    container_name: mock-server
    restart: always
    build: .
    ports:
      - 5500:5500
    networks:
      - mock_network

networks:
  mock_network:
    name: mock_network