default:
  callback: "undefined"
  uri:
    $ref: ""
  delay: 300
  payload: |
    <!DOCTYPE html>
    <html lang="en">

    <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>E-mandate Redirect</title>
      <style>
        body {
          font-family: Arial, sans-serif;
          padding: 20px;
        }
        button {
          padding: 10px 20px;
          font-size: 16px;
          cursor: pointer;
        }
        #submissionId {
          margin-top: 20px;
          font-weight: bold;
          color: purple;
        }
      </style>
    </head>

    <body>
      <p>E-mandate Redirect: This will be redirected to a different HTML page shortly.</p>
      <button onclick="showSubmissionId()">Show Submission ID</button>
      <div id="submissionId"></div>

      <script>
        function showSubmissionId() {
          const submissionId = "F06_SUBMISSION_ID"; // Replace with dynamic value if needed
          document.getElementById("submissionId").innerText = "Submission ID: " + submissionId;
        }
      </script>
    </body>

    </html>
