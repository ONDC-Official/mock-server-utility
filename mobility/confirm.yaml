default:
  callback: "on_confirm"
  uri:
    $ref: ""
  delay: 300
  payload:
    $ref: "./default/template/on_confirm.yaml"



intercity-bus:
  condition:
      operation:
        type: AND
        input:
          value:
          - operation:
              type: EQUAL
              title: Check search message code
              input:
                value:
                  - operation:
                      type: READ
                      input:
                        value: "req_body.message.order.items.0.id"
                  - ITEM_ID_1_METRO
          - operation:
                type: EQUAL
                title: Check search message code
                input:
                  value:
                    - operation:
                        type: READ
                        input:
                          value: "req_body.context.domain"
                    - "ONDC:TRV11"
  callback: "on_confirm"
  uri:
    $ref: ""
  delay: 300
  payload:
    $ref: "./metro/template/on_confirm.yaml"

    