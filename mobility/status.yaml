default:
  callback: "on_status"
  uri:
    $ref: ""
  delay: 300
  payload:
    $ref: "./default/template/error.yaml"


intercity-bus:
  condition:
      operation:
        type: AND
        input:
          value:
          - operation:
              type: EQUAL
              title: Check search message code
              input:
                value:
                  - operation:
                      type: READ
                      input:
                        value: "req_body.message.order_id"
                  - ORDER_ID_1_INTERCITY
          - operation:
                type: EQUAL
                title: Check search message code
                input:
                  value:
                    - operation:
                        type: READ
                        input:
                          value: "req_body.context.domain"
                    - "ONDC:TRV12"
  callback: "on_status"
  uri:
    $ref: ""
  delay: 300
  payload:
    $ref: "./intercity-bus/template/on_status.yaml"

metro:
  condition:
      operation:
        type: AND
        input:
          value:
          - operation:
              type: EQUAL
              title: Check search message code
              input:
                value:
                  - operation:
                      type: READ
                      input:
                        value: "req_body.message.order_id"
                  - ORDER_ID_1_METRO
          - operation:
                type: EQUAL
                title: Check search message code
                input:
                  value:
                    - operation:
                        type: READ
                        input:
                          value: "req_body.context.domain"
                    - "ONDC:TRV11"
  callback: "on_status"
  uri:
    $ref: ""
  delay: 300
  payload:
    $ref: "./metro/template/on_status.yaml"


on-demand-1:
  condition:
      operation:
        type: AND
        input:
          value:
          - operation:
              type: EQUAL
              title: Check search message code
              input:
                value:
                  - operation:
                      type: READ
                      input:
                        value: "req_body.message.order_id"
                  - ITEM_ID_1_ON_DEMAND
          - operation:
                type: EQUAL
                title: Check search message code
                input:
                  value:
                    - operation:
                        type: READ
                        input:
                          value: "req_body.context.domain"
                    - "ONDC:TRV10"
  callback: "on_status"
  uri:
    $ref: ""
  delay: 300
  payload:
    $ref: "./on-demand/template/on_status.yaml"

    