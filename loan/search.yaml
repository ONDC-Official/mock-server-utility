default:
  callback: "on_search"
  uri:
    $ref: ""
  delay: 300
  payload:
    $ref: "./default/template/on_search.yaml"


personal-loan:
  condition:
    operation:
      type: EQUAL
      title: Check search message code
      input:
        value:
          - operation:
              type: READ
              input:
                value: "req_body.message.intent.category.descriptor.code"
          - PERSONAL_LOAN
  callback: "on_search"
  uri:
    $ref: ""
  delay: 300
  payload:
    $ref: "./loan/personal-loan/template/on_search.yaml"

invoice-based-loans:
  condition:
    operation:
      type: EQUAL
      title: Check search message code
      input:
        value:
          - operation:
              type: READ
              input:
                value: "req_body.message.intent.category.descriptor.code"
          - INVOICE_BASED_LOAN
  callback: "on_search"
  uri:
    $ref: ""
  delay: 300
  payload:
    $ref: "./loan/invoice-based-loans/template/on_search.yaml"

health-based-insurance:
  condition:
    operation:
      type: EQUAL
      title: Check search message code
      input:
        value:
          - operation:
              type: READ
              input:
                value: "req_body.message.intent.category.descriptor.code"
          - HEALTH_INSURANCE
  callback: "on_search"
  uri:
    $ref: ""
  delay: 300
  payload:
    $ref: "./insurance/health-insurance/template/on_search.yaml"    

marine-based-insurance:
  condition:
    operation:
      type: EQUAL
      title: Check search message code
      input:
        value:
          - operation:
              type: READ
              input:
                value: "req_body.message.intent.category.descriptor.code"
          - MARINE_INSURANCE
  callback: "on_search"
  uri:
    $ref: ""
  delay: 300
  payload:
    $ref: "./insurance/marine-insurance/template/on_search.yaml"  

motor-based-insurance:
  condition:
    operation:
      type: EQUAL
      title: Check search message code
      input:
        value:
          - operation:
              type: READ
              input:
                value: "req_body.message.intent.category.descriptor.code"
          - MOTOR_INSURANCE
  callback: "on_search"
  uri:
    $ref: ""
  delay: 300
  payload:
    $ref: "./insurance/motor-insurance/template/on_search.yaml"      