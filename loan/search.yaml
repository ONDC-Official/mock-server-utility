default:
  callback: "on_search"
  uri:
    $ref: ""
  delay: 300
  payload:
    $ref: "./default/template/on_search.yaml"

personal-loan:
  condition:
    operation:
      type: EQUAL
      title: Check search message code
      input:
        value:
          - operation:
              type: READ
              input:
                value: "req_body.message.intent.category.descriptor.code"
          - personal-loan
  callback: "on_search"
  uri:
    $ref: ""
  delay: 300
  payload:
    $ref: "./personal-loan/template/on_search.yaml"

invoice-based-loans:
  condition:
    operation:
      type: EQUAL
      title: Check search message code
      input:
        value:
          - operation:
              type: READ
              input:
                value: "req_body.message.intent.category.descriptor.code"
          - invoice-based-loan
  callback: "on_search"
  uri:
    $ref: ""
  delay: 300
  payload:
    $ref: "./invoice-based-loans/template/on_search.yaml"

